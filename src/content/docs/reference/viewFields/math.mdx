---
title: Math
description: Math view field reference.
---

import ViewFieldInfo from '../../../../components/viewField/ViewFieldInfo.astro';
import ViewFieldAllowedArguments from '../../../../components/viewField/ViewFieldAllowedArguments.astro';
import { ViewFieldType } from '../../../../config/static.js';

<ViewFieldInfo type={ViewFieldType.MATH}></ViewFieldInfo>

A math view field performs calculations with [mathjs expressions](https://mathjs.org/docs/expressions/syntax.html).
Math view fields are the default view field, meaning that if you don't specify a view field type, it is going to default to this.

### Allowed Arguments

<ViewFieldAllowedArguments type={ViewFieldType.MATH}></ViewFieldAllowedArguments>

### Examples

```meta-bind
VIEW[{a} * {b}]
```
